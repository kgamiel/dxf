X  - remove sdict and depends
- Handle escaped ASCII control chars, ^ (see reference guide)
- separate code generation into a separate utility (?)

- autoconf installation
- Locale-based string tables(?)
